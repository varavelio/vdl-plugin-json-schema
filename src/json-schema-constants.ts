/**
 * JSON Schema draft emitted by this plugin.
 */
export const JSON_SCHEMA_DRAFT = "https://json-schema.org/draft/2020-12/schema";

/**
 * Relative output file path returned to VDL.
 */
export const OUTPUT_PATH = "schema.json";

/**
 * Fallback message used when `@deprecated` has no explicit reason.
 */
export const DEFAULT_DEPRECATED_MESSAGE =
  "This schema element is deprecated and should not be used in new code.";
